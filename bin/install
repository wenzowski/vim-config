#!/bin/bash
cd ~/.vim
[[ -s "~/.vim/bundle/vundle" ]] || git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/vundle
[[ -h "~/.vimrc" ]] || ln -s ~/.vim/vimrc ~/.vimrc
vim +BundleInstall +qall
cd ~/.vim/bundle/matcher
make && make install
